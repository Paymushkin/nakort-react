doctype html
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    meta(name="description" content=description || "Теннисный клуб - профессиональные корты, тренировки и турниры")
    meta(name="keywords" content=keywords || "теннис, корты, тренировки, турниры, спорт")
    meta(name="author" content="Теннисный клуб")
    meta(property="og:title" content=title || "Теннисный клуб")
    meta(property="og:description" content=description || "Профессиональные корты, тренировки и турниры")
    meta(property="og:type" content="website")
    meta(property="og:image" content=ogImage || "/img/og-image.jpg")
    link(rel="canonical" href=canonical || "")
    title= title || "Теннисный клуб"
    link(rel="stylesheet" href="css/main.css")
    link(rel="icon" type="image/x-icon" href="/favicon.ico")
  body
    block content
    script(src="js/main.min.js" defer)

